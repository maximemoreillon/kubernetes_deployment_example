stages:
  - stuff


stuff:
  stage: stuff
  script:
    - pwd
    - ls
    - docker version
    - docker ps
    - echo $KUBE_URL
    - echo $KUBE_TOKEN
    - kubectl get nodes
  environment:
    name: staging
    url: https://staging.maximemoreillon.com
